<% content_for :title, @project_type.typeName %>
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <p id="notice"><%= notice %></p>
                <p class="float-right">
                    <%= link_to 'Update', edit_project_type_path(@project_type), class: "btn btn-primary" %>
                    <%= link_to 'Back', project_types_path, class: "btn btn-info" %>
                </p>
                <table class="table table-striped table-bordered">
                    <tbody>
                        <tr>
                            <th>
                                <strong>Proejct Type Name:</strong>
                            </th>
                            <td>
                                <%= @project_type.typeName %>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
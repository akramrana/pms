<%= form_with(model: issue_checklist, local: true) do |form| %>
  <% if issue_checklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_checklist.errors.count, "error") %> prohibited this issue_checklist from being saved:</h2>

      <ul>
        <% issue_checklist.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: issue_image, local: true) do |form| %>
  <% if issue_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_image.errors.count, "error") %> prohibited this issue_image from being saved:</h2>

      <ul>
        <% issue_image.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

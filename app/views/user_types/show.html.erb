<% content_for :title, @user_type.userTypeName %>
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <p id="notice"><%= notice %></p>
                <p class="float-right">
                    <%= link_to 'Update', edit_user_type_path(@user_type), class: "btn btn-primary" %>
                    <%= link_to 'Back', user_types_path, class: "btn btn-info" %>
                </p>
                <table class="table table-striped table-bordered">
                    <tbody>
                        <tr>
                            <th>
                                <strong>User Type:</strong>
                            </th>
                            <td>
                                <%= @user_type.userTypeName %>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>